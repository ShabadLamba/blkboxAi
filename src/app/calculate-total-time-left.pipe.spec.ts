import { CalculateTotalTimeLeftPipe } from './calculate-total-time-left.pipe';

describe('CalculateTotalTimeLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateTotalTimeLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
